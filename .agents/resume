#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

command -v git >/dev/null
git rev-parse --is-inside-work-tree >/dev/null

printf 'Orb resume complete; no services require repair.\n'
